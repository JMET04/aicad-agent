# JLCPCB receiver-effects PCB REV A0

Upload `JLCPCB_RECEIVER_EFFECTS_REV_A0_GERBER_DRILL.zip` as a bare-PCB order. Apply the exact values in `jlcpcb-order-parameters.json`.

Electronic gates: native KiCad ERC 0/0 and DRC 0/0/0. BOM/CPL are supplied for review but PCBA is intentionally not selected in this order; several passive LCSC part numbers remain open and must be resolved before any PCBA order.
